import { test } from "@playwright/test";

test.fixme("PlansPanel filter narrows visible plans", async ({ page }) => {
  // TODO Stage 10: requires the plans-watcher backend wired to disk so
  // PlansPanel reflects real on-disk plan files and filter queries can
  // be exercised against representative fixtures.
  void page;
});
