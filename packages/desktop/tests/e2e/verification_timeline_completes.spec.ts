import { test } from "@playwright/test";

test.fixme("verification timeline lights all 5 steps", async ({ page }) => {
  // TODO Stage 10: requires a full orchestration run so the
  // VerificationTimeline can step through all 5 trust-theater stages
  // end-to-end against real hook events instead of mocked atoms.
  void page;
});
