//! SwarmCanvas — DAG visualization for the orchestration swarm.
//!
//! Replaces `@xyflow/react` with `petgraph` topological layout + a hand-rolled
//! SVG renderer. See `swarm_layout` for the layered Sugiyama-style geometry.
//!
//! Reference: `packages/desktop/src/components/SwarmCanvas.tsx`.

// TDD red: types + component land in the implementation commit (G2.D.3.3).
