# Auto-generated by ts-rs when `cargo test` runs (TS export of `Band`).
# Canonical TS bundle lives in packages/apohara-shared/types.ts via
# `bun run generate-types`. This per-crate dir is incidental test output.
bindings/
